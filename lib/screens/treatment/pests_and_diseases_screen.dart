import 'package:flutter/material.dart';

class PestsAndDiseasesScreen extends StatelessWidget {
  const PestsAndDiseasesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PestsAndDiseasesScreen')),
      body: const Center(child: Text('PestsAndDiseasesScreen Placeholder')),
    );
  }
}
