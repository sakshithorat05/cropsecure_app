import 'package:flutter/material.dart';

class PurchaseInputsScreen extends StatelessWidget {
  const PurchaseInputsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PurchaseInputsScreen')),
      body: const Center(child: Text('PurchaseInputsScreen Placeholder')),
    );
  }
}
